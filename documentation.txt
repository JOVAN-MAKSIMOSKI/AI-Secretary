MAKE THE DESIGN BETTER
Fix the long time getting to load a session or something(if needewd check
)
AGENT
Offers, transportni...
STT
RAG
Currently need to console.log everything that comes in from qdrant and debug later on, there is a script inside /services, run it to diagnose the issues, maybe an embedding model used is not good enough.
decide exactly what should be done with the agent memory and how to keep the chat exactly.
query engine built by the service? What happens?  investigate the service deeper.
The chat needs to be kept in



ensure RLS security is set up properly
add indexes for performance when querying
time zone stays fixed
consider adding a stronger LLM once finishing the app because request in macedonian might be a problem for the current ollama model.

*Add system prompts in the routers (or a dedicated services/prompts.py file) where you know the context.
For example, when you implement routers/rag.py, the RAG-specific system prompt lives there. A document generation prompt lives in routers/documents.py. That way each feature owns its own instructions and langchain.py stays reusable.
If several routers end up sharing similar prompts, you can consolidate them into a services/prompts.py file. But start in the routers — no need to abstract early.



Chat history should be set in react state. We can send older chats to context memory

Implement memory to the agent, Regarding the memory, should the agent perform based on memory or just return information.
"Write a follow-up email to Tom"
With memory: agent knows the last email was formal, 
             the invoice is 14 days overdue, 
             Tom usually replies within 2 days









fix the readme inside github to match athlys readme

.venv/Scripts/uvicorn main:app --reload --host 0.0.0.0 --port 8000


In a second terminal, open the FastAPI interactive test client (Swagger UI)
cmd.exe /c start http://127.0.0.1:8000/docs

Optional: open ReDoc
cmd.exe /c start http://127.0.0.1:8000/redoc


Semantic search could be added for the word offers/






How the "Big Players" Do ItEnterprise apps (like Slack, Microsoft Teams, or custom AI agents) use a hybrid approach:The Source of Truth (Server): All chats are stored in a secure, relational or NoSQL database. This ensures data is backed up, encrypted, and available everywhere.The Cache (Client/Redis): They use "hot" caches (like Redis on the server or IndexedDB in the browser) to make the chat feel instant.IndexedDB is the professional version of localStorage. It is asynchronous, supports large amounts of data, and handles structured queries. If you must store data on the client side, use IndexedDB, not localStorage.Synchronization: The frontend requests the latest messages from the API, and the server validates the user's session before sending the data.My Recommendation for Your "AI-Secretary"  Since you are currently in the "building/MVP" phase, using localStorage with your Zustand store is perfectly acceptable for local testing and rapid iteration. It gets your UI working today without needing to write complex database migrations.However, add a "Migration Path" to your plan:Don't build your entire product architecture around localStorage. Treat the localStorage implementation as a temporary UI cache. Eventually, when you decide to take the app to production, you can simply swap out your store/chat.ts persistence layer to fetch from an API endpoint instead of the browser's storage, and your UI components won't even know the difference!
set up railway when developing 

or each proposed change you get:

y — yes, apply it
n — no, skip it
d — show a more detailed diff before deciding
a — accept all remaining changes in this batch
e — open the file in your editor to review before decidingd