// Conditional edge logic — routes graph to next node based on state
import type { AgentState } from '../state.js';

export function router(_state: AgentState): string {
  // TODO: implement routing logic
  return 'resolver';
}
