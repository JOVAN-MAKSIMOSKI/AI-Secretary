# AI-Secretary
A multi-tenant AI secretary agent built on a TypeScript + Python monorepo. The system accepts natural language (and eventually voice) commands to generate Excel invoices and Word offer documents, send emails, and manage calendar events — all requiring human approval before any action is executed. 
